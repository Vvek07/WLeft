spring.application.name=WLeft
server.port=8080

# Database Configuration (Supabase PostgreSQL)
# Render will provide JDBC_DATABASE_URL or we set DB_URL
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:6543/postgres?prepareThreshold=0}
spring.datasource.username=${DB_USERNAME:postgres.svbgsdjlcugojvpqjnqa}
spring.datasource.password=${DB_PASSWORD:Om@123@456@789}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Razorpay Configuration
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_Rvq0QBO7CE7YSy}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:Opgre9M5N64P6pNtwhwJ5RX3}
razorpay.webhook.secret=${RAZORPAY_WEBHOOK_SECRET:my_secret_123}

# Mailtrap Configuration
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=${MAILTRAP_USERNAME:95b87cde2d5dda}
spring.mail.password=${MAILTRAP_PASSWORD:f13af88f55f566}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Initial Data Seeding
# Can be controlled via a custom property if needed, but we'll stick to checking DB count.
